<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Transparent fill -->
    <solid android:color="@android:color/transparent" />

    <!-- Border (stroke) -->
    <stroke
        android:width="1dp"
        android:color="#B0B0B0" />  <!-- Change color as needed -->
</shape>
